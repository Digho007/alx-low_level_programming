#include <stdio.h>
#include "main.h"

/**
 * _abs - computes absolute value of an integer
 * @int: input
 * Return: 0 signifies success
 */

int _abs(int)
{
	if (int < 0)
		int *= -1;
		printf("%d\n", int);
	else if (int > 0)
		printf("%d\n", int);
	return (0);
}
