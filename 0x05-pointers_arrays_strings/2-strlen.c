#include "main.h"

/**
 * _strlen - returns the length of a string
 * @s: input; *s: pointer to a string
 * Return: Always 0
 */

int _strlen(char *s)
{
	int i;

	for (i = o; s[i] != '\0'; i++)
		i += i;
	return (i);
}
