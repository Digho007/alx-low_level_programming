#include <stdio.h>

/**
 * main - prints all possible combinations of two-digit numbers.
 * Return: 0 means successful
 */

int main(void)
{
	int a, b;

	for (a = '0'; a <= 98; a++)
	{
		for (b = '1'; b <= '99'; b++)
		{
			if (a < b)
			{

			}
		}
	}
}
